[project]
name = "iac-security-auditor"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.111.0", "uvicorn[standard]>=0.29.0", "pydantic>=2.7.0",
    "pydantic-settings>=2.2.0", "redis[hiredis]>=5.0.0", "asyncpg>=0.29.0",
    "httpx>=0.27.0", "openai>=1.30.0", "anthropic>=0.30.0",
    "celery>=5.4.0", "pygithub>=2.9.1", "pyyaml>=6.0",
    "python-dotenv>=1.0.0", "tenacity>=8.3.0", "jinja2>=3.1.0",
]

[tool.ruff]
line-length = 100
